services:
  - type: web
    name: lista-casamento-neiva-aldenio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn apppy:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.14.0